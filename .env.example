# Optional. GitHub unauthenticated requests work for small demos, but a token
# gives higher rate limits. Use a token with minimum required permissions.
GITHUB_TOKEN=

# Optional. Reserved for future summarization providers.
OPENAI_API_KEY=

# Output directory for generated logs.
WORKNODE_DATA_DIR=worknode-data
